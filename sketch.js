let x
let y 
let d
let Vx
let Vy

function setup() 
{
    x = random(400)
    y = random(400)
    d = random(25,64)
    Vx = - 10
    Vy = 20
    createCanvas(400, 400);
}

function draw()
{

}


function fiore(x,y,d)
{
  
  let x1 = x - d/2
  
  let x2 = x + d/2

  let y1 = y - d/2
  
  let y2 = y + d/2
  
}